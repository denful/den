{
  "planPath": "docs/superpowers/plans/2026-05-06-pipes-remaining-work.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: pipe.withProvenance + Config-Dependent Thunks",
      "status": "pending",
      "blockedBy": [],
      "description": "Provenance wrapping, config thunk detection, lazy forward ref resolution.",
      "metadata": {
        "files": ["nix/lib/aspects/fx/assemble-pipes.nix", "nix/lib/aspects/fx/resolve.nix", "templates/ci/modules/features/pipe-scope.nix"],
        "verifyCommand": "nix develop -c just ci",
        "acceptanceCriteria": ["provenance wrapping works", "config thunks resolved lazily", "mutual dependencies work"],
        "note": "Blocked on fleet scope-tree migration completing (separate session)"
      }
    },
    {
      "id": 2,
      "subject": "Task 2: Pipeline-time discriminator deferral for pipe args",
      "status": "pending",
      "description": "bind.nix recognizes pipe args, defers discriminators, post-assembly drain pass.",
      "metadata": {
        "files": ["nix/lib/aspects/fx/handlers/bind.nix", "nix/lib/aspects/fx/resolve.nix", "templates/ci/modules/features/pipes.nix"],
        "verifyCommand": "nix develop -c just ci",
        "acceptanceCriteria": ["discriminator deferred until siblings emit", "order-independent results", "empty pipe gets []"]
      }
    },
    {
      "id": 3,
      "subject": "Task 3: Rename den.pipes to den.quirks + ref ergonomics",
      "status": "pending",
      "description": "Rename option, add name field to schema type, pipe.from accepts refs.",
      "metadata": {
        "files": ["modules/options.nix", "nix/nixModule/pipes.nix", "nix/nixModule/default.nix", "nix/lib/aspects/fx/key-classification.nix", "nix/lib/policy-effects.nix", "templates/ci/modules/features/pipes.nix", "templates/ci/modules/features/pipe-policy.nix", "templates/ci/modules/features/pipe-scope.nix"],
        "verifyCommand": "nix develop -c just ci",
        "acceptanceCriteria": ["den.quirks option works", "pipe.from accepts refs", "string syntax still works", "all tests pass"]
      }
    }
  ],
  "lastUpdated": "2026-05-06T00:00:00Z"
}
